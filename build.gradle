apply plugin: 'java'
sourceSets {
    main {
        java {
            srcDir 'src'
            exclude '**/tests/**'
        }
    }
}

dependencies {
    compile fileTree(dir: 'jar', include: '*.jar')
}
